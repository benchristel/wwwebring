
export function model(config) {
  return {
    prevUrl: config.hub,
    prevTitle: config.name,
    hubUrl: config.hub,
    hubTitle: config.name,
    nextUrl: config.hub,
    nextTitle: config.name,
  }
}